You can build the rpm by running the build script:

ulyaoth-wolfssl:
wget https://raw.githubusercontent.com/ulyaoth/repository/master/ulyaoth-wolfssl/build-wolfssl.sh ; chmod +x build-wolfssl.sh ; ./build-wolfssl.sh